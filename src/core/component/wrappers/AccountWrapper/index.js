// @flow
import React from 'react'

// connect users,groups, projects
export default class AccountWrapper extends React.Component<*, *, *> {
	componentWillMount () {
		// fetch users, groups, projects if not available
	}

	render () {
		return (
			null
		)

	}
}

